package org.latinschool;

import com.badlogic.gdx.Gdx;
import com.badlogic.gdx.graphics.Texture;
import com.badlogic.gdx.graphics.g2d.Sprite;
import com.badlogic.gdx.maps.tiled.TiledMapTileLayer;

public class Ghost extends Sprite {
    /*int ghostX;
    int ghostY;
    MyPacManGame game;

    private float ghostMoveTime = 0.06f; // 0.5 seconds pause (adjustable)
    private float ghostMoveTimer = 0f;

    Direction direction;
    public Ghost(Direction startingDirection, MyPacManGame game) {
        super();
        this.direction = startingDirection;
        this.ghostX = 24;
        this.ghostY = 24;
        this.game = game;

    }
    public Ghost(Texture texture, Direction startingDirection, int ghostX, int ghostY, MyPacManGame game) {
        super(texture);
        this.direction = startingDirection;
        this.ghostX = ghostX;
        this.ghostY = ghostY;
        this.game = game;
    }
    public void move(){
        float delta = Gdx.graphics.getDeltaTime();
        // Only allow movement if the ghost move timer has finished
        if (ghostMoveTimer > 0) {
            ghostMoveTimer -= delta;
            return;
        }
        // Get current position in tiles
        int tileX = (int) (ghostX / wallLayer.getTileWidth());
        int tileY = (int) (ghostY / wallLayer.getTileHeight());

        //switch (direction) {
          //  case UP:
                //if (!isCo)
       // }
    }*/

}
